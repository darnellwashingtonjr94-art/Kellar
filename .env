# 1. Create your local .env file with your Gemini API key
cat << 'EOF' > .env
GEMINI_API_KEY=your_actual_gemini_api_key_here
EOF

# 2. Build and start the entire Kellar ecosystem using Docker Compose
docker-compose up --build
